#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

#define b100 100
#define b50 50
#define b10 10
#define b5 5
#define b1 1


int calculador_de_dinero(int dineroEntregar);

int main(void) {
	int dineroEntregar = 0;
	printf("Ingrese la cantidad de dinero que desea retirar (0 para finalizar): \n");
	scanf("%d", &dineroEntregar);
	int calculador_de_dinero(dineroEntregar);
}

int calculador_de_dinero(int dineroEntregar) {
	if (dineroEntregar != 0) {
		b100 / 100;
		if()
	}
}